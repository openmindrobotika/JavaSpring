package ejercicio1;

/* Crear un proyecto Ejercicio1 en el que tendrás que crear la(s) clase(s) que haga lo siguiente: 
Escribir un método que devuelve Tom a los múltiples de tres y, Jerry a los múltiples de 5. 
A los números que son a la vez múltiples de 5 y de 3, el método debe devolver Tom y Jerry. 
A los demás números, devolver el número indicado. 
*/
public class Main { // Clase principal

/**
	 * Este método se encarga de iniciar la ejecución del programar
	 * Éste es el método principal del proyecto
	 * @param args[] es un arreglo con los parámetros que el reciba por consola
	 * @return void
	 * @author Jose Maria Correa
	*/	
	public static void main(String[] args) {
		// METODO PRINCIPAL
		new ClaseConMetodos(); // creo una instancia de la ClaseConMetodos y se ejecuta al tener esta un constructor
	}

}
